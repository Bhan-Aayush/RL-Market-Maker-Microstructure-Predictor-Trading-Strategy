# Minimal requirements for Dashboard only
# This avoids all Python 3.13 compatibility issues

# Dashboard essentials
plotly>=5.18.0
requests>=2.31.0
streamlit>=1.29.0

# Trading interface (if not already installed)
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
websockets>=12.0
pydantic>=2.5.0
